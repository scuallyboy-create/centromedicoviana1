/* Viana I — controlo global. O estado real é mantido em Supabase quando configurado.
   Fallback local: SITE_ATIVO = true. O administrador deve usar admin.html para alterar o estado. */
window.SITE_ATIVO = true;
